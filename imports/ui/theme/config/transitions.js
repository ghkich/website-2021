const COLOR_DELAY = '0.15s'
const COLOR_EFFECT = 'linear'

export default {
  COLORS: `color ${COLOR_DELAY} ${COLOR_EFFECT}, background-color ${COLOR_DELAY} ${COLOR_EFFECT}, border-color ${COLOR_DELAY} ${COLOR_EFFECT}`,
}
